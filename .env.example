# App Store Connect API Credentials
# Get these from: App Store Connect → Users & Access → Integrations → App Store Connect API

# Key ID — shown next to your key name
ASC_KEY_ID=XXXXXXXXXX

# Issuer ID — shown at the top of the API keys page
ASC_ISSUER_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx

# Path to .p8 private key file (downloaded when you created the key)
ASC_KEY_PATH=./AuthKey_XXXXXXXXXX.p8

# App ID — App Store Connect → your app → General → App Information → Apple ID
ASC_APP_ID=1234567890
